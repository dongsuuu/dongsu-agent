services:
  - type: web
    name: dongsu-api
    runtime: python
    plan: free
    buildCommand: pip install fastapi uvicorn
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    rootDir: api
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
